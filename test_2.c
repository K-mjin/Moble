/* 사용자로부터 문자를 하나 입력받아 모음인지 자음인지 
구분하는 코드를 작성하시오. 단 switch문을 사용한다. */
#include <stdio.h>
int main() {
	int text;
	
	printf("문자를 쓰시오.");
	scanf("%d", &text);

	switch (text) {
	case 'a':
	case 'i':
	case e:
	case o:
	case u:

	}

}