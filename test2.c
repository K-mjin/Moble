/* 덧셈, 뺄셈, 곱셈, 나눗셈을 지원하는 계산기 프로그램을
구현하시오. 이때 각 연산들이 몇번씩 게산하게되었는지를
기억하게 하고 각 연산을 지원하는 함수들은 자신이 호출된
횟수를 화면에 출력하도록 하시오. */
#include <stdio.h>

int add(int, int);
int sub(int, int);
int mut(int, int);
int div1(int, int);

int main(void) {
	char op;
	int x, y;
	int i;

	for (i = 0; i < 5; i++) {

	}
}